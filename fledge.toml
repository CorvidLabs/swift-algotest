# fledge.toml — project task definitions
[tasks]
build = "sh -c 'trap \"git restore Package.resolved\" EXIT; swift build'"
test = "sh -c 'trap \"git restore Package.resolved\" EXIT; swift test'"

[lanes.verify]
description = "Build and test AlgoTest without implicitly starting live sandbox infrastructure"
steps = ["build", "test"]
